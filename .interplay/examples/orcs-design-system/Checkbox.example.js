import React from "react";
import { Checkbox } from "orcs-design-system";

export default {
  title: "Checkbox",
  component: Checkbox
};

export const Basic = {
  render: () => <Checkbox onChange={() => {}} label="Default checkbox" />
};
