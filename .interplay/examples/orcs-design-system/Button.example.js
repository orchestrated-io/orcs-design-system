import React from "react";
import { Button } from "orcs-design-system";

export default {
    title: "Button",
    component: Button,
};

export const Basic = { render: () => <Button>Default button</Button> };