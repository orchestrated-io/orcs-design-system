import { Story, Preview, Props } from "@storybook/addon-docs/blocks";
import Select from "../Select";

# Select

[Default](#default) | [Multi-select](#multi-select) | [Inverted](#inverted) | [Properties](#properties)

This component uses React Select: <https://react-select.com/home>

Usage of this component has changed since we have upgraded to the latest version of `react-select`. For example, field values are now defined as separate objects. for example:

       const options = [
      { value: 'caramel', label: 'Caramel' },
      { value: 'chocolate', label: 'Chocolate' },
      { value: 'strawberry', label: 'Strawberry' },
      { value: 'vanilla', label: 'Vanilla' }
     ]

For a full list of the changes, see <https://react-select.com/upgrade-guide>.

## Default

<Preview>
  <Story id="components-select--default-select" />
</Preview>

## Specific Height Select

With height and padding props, you can select custom height and padding of select control. Be sure to include unit in the string, e.g. px.

<Preview>
  <Story id="components-select--specific-height-select" />
</Preview>

## Multi-Select

<Preview>
  <Story id="components-select--multi-select" />
</Preview>

## Multi-Select with fixed options

<Preview>
  <Story id="components-select--multi-select-with-fixed-options" />
</Preview>

## Inverted

<Preview>
  <Story id="components-select--inverted-select" />
</Preview>

## With `data-testid` attribute

<Preview>
  <Story id="components-select--with-data-id-select" />
</Preview>

## With selected defaultValue

This uses a custom `util`. To use, add `import { getOptionByValue } from "orcs-design-system"` to your imports.

<Preview>
  <Story id="components-select--with-selected-value-select" />
</Preview>

## With createable

<Preview>
  <Story id="components-select--with-createable-select" />
</Preview>

## Properties

## With grouped options

(Auto-expanded to show grouped options)
<Preview>
  <Story id="components-select--with-grouped-options" />
</Preview>

## Properties


<Props of={Select} />
