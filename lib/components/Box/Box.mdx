import { Story, Meta, Controls, Canvas } from "@storybook/addon-docs";
import { Box } from "../Box";
import * as stories from "./Box.stories";

<Meta of={stories} />

# Box

[Default Box](#default) | [Border](#border) | [Shadow](#shadow) | [Background colour](#background-colour) | [Padding](#padding) | [Border radius](#border-radius) | [Properties](#properties) | [Upgrading](#upgrading)

Component to contain elements within a simple box. If your `Box` needs to contain several items and you want to control the item spacing and positioning, it's highly recommended to use `Grid` or `Flex` instead.

## Default

<Canvas of={stories.basicBox} />

## Border

<Canvas of={stories.borderBox} />

## Shadow

<Canvas of={stories.shadow} />

## Background colour

<Canvas of={stories.backgroundColour} />

## Padding

<Canvas of={stories.padding} />

## Border Radius

<Canvas of={stories.borderRadius} />

## Properties

You can use `space`, `layout`, `border`, and `color` properties with `Box`.

### For reference, see:

- [space on Styled-System](https://styled-system.com/table#space)
- [layout on Styled-System](https://styled-system.com/table#layout)
- [border on Styled-System](https://styled-system.com/table#border)
- [color on Styled-System](https://styled-system.com/table#color)
- [systemtheme.js](https://github.com/orchestrated-io/orcs-design-system/blob/master/packages/orcs-design-system/lib/systemtheme.js) (our arrays and scales for design system values)

By default, we've set certain values on `Box` properties as documented in the table below.

<Controls />

**Note:** If specific `margin` or `padding` properties fail to override the defaults, specify the overall property and then the specific property. For example, to set a `Box` with a `margin-bottom: 8px`, use

`<Box m={0} mb={3}>`
