import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import Box from "../Box";
import Tooltip from "../Tooltip";
import { TabsContainer, Tab } from ".";

<Meta title="Components/Tabs" component={TabsContainer} />

# Tabs

Tabs are intended to provide a way to navigate between distinct sections within the layout without leaving the page or triggering a page refresh.

<Preview>
  <Story name="Basic">
    <Box bg="greyLightest" p={4}>
      <TabsContainer>
        <Tab>
          <a href="#">
            Details
            <Tooltip direction="top">
              Example with tooltip explaining tab
            </Tooltip>
          </a>
        </Tab>
        <Tab active>
          <a href="#" tabIndex="-1">
            Additional information
          </a>
        </Tab>
        <Tab notification="97">
          <a href="#">Planning</a>
        </Tab>
      </TabsContainer>
    </Box>
  </Story>
</Preview>
