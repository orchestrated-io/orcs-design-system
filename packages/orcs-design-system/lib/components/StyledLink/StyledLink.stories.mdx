import * as stories from './StyledLink.stories.js';
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import StyledLink from ".";
import Box from "../Box";

<Meta title="Components/StyledLink" component={StyledLink} decorators={[storyFn => <Box p="r">{storyFn()}</Box> ]} />

# StyledLink

[Default](#default) | [Inverted](#inverted) | [Properties](#properties)

This `StyledLink` component supports both standard html hyperlinks and react Link components (if using react router for example).

The way to use this as a hyperlink is shown below in the examples and is quite straightforward.

To wrap a react router Link component in these styles, you need to do the following:

Ensure you have imported both the react router link, and the design system `styleLink` function, like so:

    import { Link } from 'react-router-dom';
    import { styleLink } from 'orchestrated-design-system';

At the top of your file give a name to the link, for example, lets say we are putting these links in a header component, so we'll call it HeaderLink:

```const HeaderLink = styleLink(Link);```

Then you can use in your code like so:

    <HeaderLink to={PATHS.DASHBOARD}>
      Dashboard
    </HeaderLink>

## Default

<Preview>
  <Story name="defaultStyledLink">{stories.defaultStyledLink()}</Story>
</Preview>

## Inverted

<Preview>
  <Story name="invertedStyledLink">{stories.invertedStyledLink()}</Story>
</Preview>

## Custom
Custom react-router link using the `HeaderLink` example above

## Properties

<Props of={StyledLink} />
