import * as stories from './TextArea.stories.js';
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import TextArea from ".";
import Box from "../Box";

<Meta title="Components/TextArea" component={TextArea} decorators={[storyFn => <Box p="r">{storyFn()}</Box> ]} />

# TextArea

[Default](#default) | [Size](#size) | [Invalid](#invalid) | [Valid](#valid) | [Full-width](#full-width) | [Inverted](#inverted) | [Properties](#properties)

The TextArea component should be used wherever a multi line form entry is required. Some examples of this could be enquiries, descriptions, summaries, questions etc.

Examples include three validation states: default, invalid, valid.

Ensure to use a unique id for each text area.

Specify size using `cols` and `rows` like shown in the example. adding `fullWidth` prop will override the cols value and make the text input 100% width of parent container.


## Default

<Preview>
  <Story name="defaultTextArea">{stories.defaultTextArea()}</Story>
</Preview>

## Size

<Preview>
  <Story name="sizeTextArea">{stories.sizeTextArea()}</Story>
</Preview>

## Invalid

<Preview>
  <Story name="invalidTextArea">{stories.invalidTextArea()}</Story>
</Preview>

## Valid

<Preview>
  <Story name="validTextArea">{stories.validTextArea()}</Story>
</Preview>

## Full-width

<Preview>
  <Story name="fullWidthTextArea">{stories.fullWidthTextArea()}</Story>
</Preview>

## Inverted

<Preview>
  <Story name="invertedTextArea">{stories.invertedTextArea()}</Story>
</Preview>

## Properties

<Props of={TextArea} />
