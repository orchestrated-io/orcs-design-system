import * as stories from './ActionsMenu.stories.js';
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import ActionsMenu from ".";
import Box from "../Box";

<Meta title="Components/ActionsMenu" component={ActionsMenu} decorators={[storyFn => <Box p="20px 20px 80px 20px">{storyFn()}</Box>]} />

# Actions Menu

[Default actions menu](#default-actions-menu) | [Left offset actions menu](#left-offset-actions-menu) | [Properties](#properties)

This component should be used when an item in the UI has multiple actions that can be taken on it.

The child components that are accepted by this component are standard html hyperlinks <a> or buttons <button>, or react router <Link> depending on what the function of the menu item needs to be.

## Default Actions Menu

<Preview>
  <Story name="defaultActionsMenu">{stories.defaultActionsMenu()}</Story>
</Preview>

## Left offset actions menu

<Preview>
  <Story name="leftOffsetActionsMenu">{stories.leftOffsetActionsMenu()}</Story>
</Preview>

## Properties

<Props of={ActionsMenu} />
